# Integração

```bash
npm run test:integration
```

Casos em `tests/integration/**/*.integration.test.js` (Jest project `integration`, mesmo harness MongoMemory da API).

Cobertura adicional de isolamento: `npm run test:isolamento`.

No CI (TOKEN 16): grupo **NORMAL** em cada PR.
