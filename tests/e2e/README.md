# E2E Playwright (TOKEN 13)

```bash
npx playwright install chromium
npm run test:e2e:pw
```

Servidor sobe automaticamente (MongoMemory + seed) na porta `3013`.

Relatório: `docs/E2E-PLAYWRIGHT-RELATORIO.md`
